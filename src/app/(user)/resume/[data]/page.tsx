import { ResumeFormInput } from '@/types/resume';
import React from 'react';

type Props = {
	params: ResumeFormInput;
};

const Page = ({ params }: Props) => {
	return <div>Page</div>;
};

export default Page;
